#include "AssetsManager.h"
#include "Api.h"

// uint32_t AssetsManager::useMeshAsset(uint32_t id_) 
// {
	// Asset<Mesh>& rAsset = this->ModelAT.assets.get(id_);
	// Mesh& rMesh = rAsset.resource; 
	// Create Vertex Buffer and fill it with data.
	
	// rMesh.vbMemoryId = addEntry(rAsset.name.c_str(), sizeof(Vertex) * rMesh.vertices.size(), VK_BUFFER_USAGE_VERTEX_BUFFER_BIT, VK_SHARING_MODE_EXCLUSIVE, false); 
	// Demo::GPUMemoryManager.upload(rMesh.vbMemoryId, rMesh.vertices.data(), Demo::renderer.mainDevice.queues.graphicsQueue); 
		
	// Create Index Buffer and fill it with data.
	// rMesh.ibMemoryId = Demo::GPUMemoryManager.addEntry(rAsset.name.c_str(), sizeof(rMesh.indices), VK_BUFFER_USAGE_INDEX_BUFFER_BIT, VK_SHARING_MODE_EXCLUSIVE, false); 
	// Demo::GPUMemoryManager.upload(rMesh.ibMemoryId, rMesh.indices.data(), Demo::renderer.mainDevice.queues.graphicsQueue); 
// }


// void AssetsManager::useTextureAsset(uint32_t id_) 
// {


// }

